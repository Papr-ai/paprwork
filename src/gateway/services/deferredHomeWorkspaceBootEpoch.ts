/**
 * Bump when deferred home/workspace boot logic changes (repair, projection, invariants).
 * Stored per-namespace in gateway-deferred-boot.json — no env vars.
 */
export const DEFERRED_HOME_WORKSPACE_BOOT_EPOCH = 1;
