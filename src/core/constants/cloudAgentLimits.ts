/** Wall-clock timeout for cloud agent job runs (desktop → memory → gateway). */
export const CLOUD_AGENT_JOB_TIMEOUT_MS = 1_800_000; // 30 minutes

/** Memory server httpx timeout when calling cloud agent gateway. */
export const CLOUD_AGENT_GATEWAY_TIMEOUT_SEC = 1_800; // 30 minutes
